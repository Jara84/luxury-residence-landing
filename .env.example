# Copiar a .env.local para desarrollo, y crear la variable VITE_GA_ID
# en GitHub (Settings > Secrets and variables > Actions > Variables) para producción.
# Si queda vacío, la página funciona igual pero no envía datos a Google Analytics.
VITE_GA_ID=
